ReadMe
1. The SVM is programmed using python 2.7 and can be run using the command, “python SVM.py”. Given that the “python-libsvm”  and “package is installed .
2. The LeNet is programmed using Python 3 and can be run using the command “python3 LeCUNN.py”. Given that the packages of tensorflow is installed.
3. The 5 layered network and the VGG network are in the same file “transferlearning.py” and is programmed using python 3 and can be run using the command “python3 transferlearning.py”. Given that the “Keras” package is installed.
4. The data is said to be in the same folder as that of the program to execute or change the directory path inside the code to the data folder.
